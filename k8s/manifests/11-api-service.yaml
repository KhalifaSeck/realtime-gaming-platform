apiVersion: v1
kind: Service
metadata:
  name: rtgaming-api
  namespace: rtgaming
  labels:
    app: rtgaming-api
spec:
  type: ClusterIP
  selector:
    app: rtgaming-api
  ports:
    - port: 8000
      targetPort: 8000
      name: http