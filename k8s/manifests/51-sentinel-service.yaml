apiVersion: v1
kind: Service
metadata:
  name: rtgaming-sentinel
  namespace: rtgaming
  labels:
    app: rtgaming-sentinel
spec:
  type: ClusterIP
  selector:
    app: rtgaming-sentinel
  ports:
    - port: 8888
      targetPort: 8888
      name: http